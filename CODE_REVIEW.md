# mermark 코드 리뷰

작성 기준: 2026-08-09 현재 작업 트리

## 1. 역사적 리뷰 (2026-08-09)

> 이 절과 원래 발견사항은 2026-08-09 당시의 기준선이다. 이후 구현과 검증 결과는 아래 날짜가 붙은 후속 절에 기록하며, 이 역사적 기록의 문장을 현재 상태로 해석하지 않는다.

macOS 단일 사용자 로컬 앱을 기준으로 현재 소스와 미커밋 상태를 검토했다. 동기화·협업·클라우드 기능은 범위에서 제외했고, 자동저장은 저장 주기와 정상 저장 동작만 평가했다. 저장되지 않은 편집 내용의 복구 설계는 평가하지 않았다.

가장 큰 제품 리스크는 현재 구현과 요구되는 볼트 워크스페이스 경험의 간극이다. 현재 앱은 문서 1개를 창 1개로 여는 구조이며, 탐색기·파일 찾기·최근 문서·즐겨찾기는 추가되어 있지만 볼트 선택, 탭, 탭별 상태 보존이 없다. 이 구조는 README와 설계 문서에도 명시된 현재 범위이므로 단순 결함이라기보다 다음 제품 단계의 기능 공백이다.

우선순위는 다음과 같다.

1. P0: 볼트 워크스페이스와 탭 모델 도입 여부를 확정하고, 도입한다면 파일 전환과 기존 자동저장 경계를 탭 단위로 재설계한다.
2. P1: 탐색기·파일 찾기의 큰 폴더 동작, 권한 오류 표시, 검색 범위를 명확히 한다.
3. P1: 빌드 경고와 번들 크기를 줄여 첫 실행과 Mermaid/뷰어 사용 비용을 낮춘다.
4. P2: 외부 변경 감지, 빈 상태, 키보드 접근성의 수동 시나리오를 실제 macOS 앱 테스트로 확장한다.

### 2026-08-10 후속 구현 상태 (역사적 스냅샷)

이 절은 이 문서 작성 뒤 반영된 `5d77767`(볼트 탭·충돌 복구) 및 `29d5662`(즐겨찾기→볼트 이전), 그리고 v0.10.1의 당시 소스를 대조한 결과다. 원래 발견사항을 지우지 않고 당시 처리 상태와 남은 작업을 명시한다. 2026-08-11 task 1 및 task 8–12 검증 결과가 반영된 현재 판정은 다음 절을 기준으로 한다.

| 항목 | 현재 상태 | 판정 근거 | 다음 조치 |
| --- | --- | --- | --- |
| F-01 탭·탭별 상태 | 부분 해결 | `src/workspace/vault-tabs.ts`가 볼트별 탭·활성 탭을 저장하고, `src/main.ts`가 전환 전 저장을 수행한다. 다만 탭별 살아있는 에디터 인스턴스/미저장 버퍼 모델은 없다. | 탭 전환·닫기·충돌 시 상태 계약을 확정하고 실제 앱 시나리오로 검증한다. |
| F-02 볼트 선택·루트 영속화 | 해결 | `WorkspaceStore`의 영구 볼트와 비영속 글로벌 볼트, 탐색기 루트 고정이 구현됐다. | 회귀 테스트 유지. |
| F-03 파일 열기 오류 안내 | 부분 해결 | 경로 입력은 오류를 반환하고 환영 화면의 최근 문서는 한국어 오류를 표시한다. 초기 인자 실패는 아직 영문 단문이며 탐색기/검색 실패는 일관된 UI가 아니다. | F-06/F-10과 함께 오류 상태와 재시도 UI를 통합한다. |
| F-04 자동저장 전환·종료 경계 | 해결 | `commitBeforeSwitch()`와 Tauri close 요청 저장 경로가 있으며 충돌 시 복구 파일 경로를 둔다. | 실제 macOS 바이트 확인 smoke를 F-08에서 추가한다. |
| F-05 본문·태그 검색 | 미처리 | `src/sidebar/search/search-panel.ts`와 `list_files_recursive`는 파일명·상대 경로 검색만 수행한다. | 파일 찾기 명칭을 유지하거나 별도 본문 검색 기능을 설계한다. |
| F-06 탐색기 권한 오류·재시도 | 미처리 | `explorer-panel.ts`의 `readChildren()`가 `listDir` 실패를 빈 배열로 캐시한다. | 폴더별 오류 행·캐시 무효화·재시도·루트 재선택 CTA를 구현한다. |
| F-07 대용량 스캔·캐시 최신성 | 미처리 | Rust의 깊이/10,000개 제한과 프런트엔드 캐시가 유지된다. | 명시적 새로고침부터 제공하고, 측정 후 백엔드 작업·진행률·취소를 설계한다. |
| F-08 실제 macOS 키보드 검증 | 미처리 | 단위 테스트와 브라우저 UI 확인은 있으나 WKWebView에서의 포커스·Cmd 경로 smoke 근거가 없다. | Tauri 앱에서 키보드·자동저장·탭 시나리오를 자동/수동 smoke로 고정한다. |
| F-09 빈 상태·첫 작업 안내 | 해결 | 글로벌 볼트와 탐색기 진입 경로가 문서 없는 시작 상태를 제공한다. | 회귀 테스트 유지. |
| F-10 오류 후 복구 경로 | 부분 해결 | 외부 변경은 유지/디스크 버전/병합 선택을 제공한다. 삭제·권한 변경·탐색기 읽기 실패는 아직 표준화되지 않았다. | F-03/F-06과 하나의 오류·복구 모델로 묶는다. |
| Q-01 CSS minifier 경고 | 미처리 | 2026-08-10 `npm run build`에서 `Unexpected "*"`, `Unterminated string token`이 재현됐다. | 주석의 종료 토큰 형태를 제거하고 경고 0건을 검증한다. |
| Q-02 초기 번들 크기 | 미처리 | 같은 빌드에서 index 1.01 MB, Mermaid 622 KB, Wardley 615 KB 등 경고가 유지됐다. | 사용자 지연 로딩 경로를 측정·분리하고 성능 예산을 도입한다. |
| Q-03 Rust 가시성·의존성 | 부분 해결 | `epub_frame_csp`는 `pub(crate)`로 조정됐다. 단, production `npm audit`에는 중간 2건·높음 1건이 남았다. | 취약점별 상향 가능성·뷰어 회귀를 검토해 업데이트한다. |
| Q-04 mtime 외부 변경 감지 | 미처리 | `watcher.rs`와 쓰기 충돌 검사가 mtime 기준이며 읽기 불가·삭제 이벤트는 조용히 무시한다. | 이벤트 뒤 내용 비교 또는 크기+해시 보조 신호를 설계한다. |

현 시점 집계는 해결 3건(F-02, F-04, F-09), 부분 해결 4건(F-01, F-03, F-10, Q-03), 미처리 7건(F-05~F-08, Q-01, Q-02, Q-04)이다. 빌드는 성공하지만 경고 없는 빌드 기준은 아직 충족하지 않는다.

### 2026-08-10 후속 실행 우선순위와 배치 (역사적 스냅샷)

| 순서 | 작업 묶음 | 포함 항목 | 완료 기준 |
| --- | --- | --- | --- |
| P0-1 | 빌드 신뢰성 복구 | Q-01 | production build에서 CSS 경고 0건. |
| P0-2 | 파일시스템 오류·복구 모델 | F-03, F-06, F-10 | 파일 열기·폴더 열람·외부 변경/삭제 각각에 원인·재시도·안전한 다음 행동이 보인다. |
| P1-1 | 워크스페이스 전환 완성 | F-01, F-08 | 탭 전환/닫기/충돌/종료의 상태 계약과 macOS 키보드·저장 smoke가 통과한다. |
| P1-2 | 탐색기 최신성 | F-07 | 사용자가 현재 루트를 명시적으로 새로고침할 수 있고, 제한/오류 상태가 결과와 구분된다. |
| P2-1 | 검색 범위 결정·구현 | F-05 | 파일 찾기와 본문 검색의 제품 계약을 분리하고, 선택한 범위에 맞는 결과·제한·이동을 제공한다. |
| P2-2 | 초기 로드 성능 | Q-02 | 실제 사용 시점 lazy load와 성능 예산으로 초기 번들 경고를 줄인다. |
| P2-3 | 장기 신뢰성 | Q-03, Q-04 | 취약점 triage 결과와 외부 변경 감지 보조 신호의 설계/검증이 남는다. |

P0-2가 이후 파일 열기·탭·탐색기 작업의 공통 오류 표면을 제공하므로, 기능을 개별적으로 덧대기 전에 먼저 처리한다. 본문 검색 방식과 mtime 대체 신호는 사용자에게 보이는 범위·성능·충돌 정책을 결정해야 하므로 해당 실행 묶음의 설계 단계에서 확정한다.

실행 계획 초안은 [`.omo/drafts/code-review-remediation.md`](.omo/drafts/code-review-remediation.md)에 있으며, 현재 `awaiting-approval` 상태다. 승인 뒤 같은 슬러그의 [`.omo/plans/code-review-remediation.md`](.omo/plans/code-review-remediation.md)로 실행용 계획을 생성한다.

### 2026-08-11 현재 상태: 증거 기반 조정

아래 표가 현재 소스와 task 1, 8–12의 독립 산출물을 대조한 현행 판정이다. `해결`은 해당 범위의 수락 시나리오가 통과했다는 뜻이고, `부분`은 검증된 경계와 남은 경계를 함께 적은 것이다. `보류`는 구현 승인이 없는 별도 결정 게이트이며, 과거 발견을 삭제하거나 해결로 세지 않는다.

| 항목 | 현재 판정 | 현재 근거와 남은 경계 |
| --- | --- | --- |
| F-01 탭·탭별 상태 | 부분 | `VaultTabStore`와 전환·닫기 트랜잭션은 [task 6](.omo/evidence/code-review-remediation/task-6.md) 및 통합 [task 8](.omo/evidence/code-review-remediation/task-8.md)에서 검증했다. 현재 에디터는 하나를 재사용하므로 탭별 살아있는 에디터/버퍼 인스턴스는 주장하지 않는다. |
| F-02 볼트 선택·루트 영속화 | 해결 | 현재 `WorkspaceStore`/workspace sidebar와 루트 고정은 task 6·8의 전환/패널 시나리오에서 통과했다. |
| F-03 파일 열기 오류 안내 | 해결 | [task 4](.omo/evidence/code-review-remediation/task-4.md)의 open/recovery 계약과 [task 8](.omo/evidence/code-review-remediation/task-8.md)의 실제 거부-open 화면이 원인·취소·버퍼 보존을 검증한다. |
| F-04 자동저장 전환·종료 경계 | 해결 | [task 6](.omo/evidence/code-review-remediation/task-6.md)와 [task 8](.omo/evidence/code-review-remediation/task-8.md)의 전환·종료·디스크 바이트 시나리오가 통과했다. |
| F-05 본문·태그 검색 | 보류 | 현재 동작은 파일명/상대경로 찾기뿐이다. [검색 결정 게이트](docs/reviews/full-text-search-decision-2026-08-11.md)에서 **not approved for implementation**으로 명시했으며 본문 검색을 구현했다고 주장하지 않는다. |
| F-06 탐색기 권한 오류·재시도 | 해결 | [task 5](.omo/evidence/code-review-remediation/task-5.md)와 [task 8](.omo/evidence/code-review-remediation/task-8.md)가 빈 폴더·루트 오류·자식 재시도·새로고침을 별도 상태로 검증했다. |
| F-07 대용량 스캔·캐시 최신성 | 부분 | task 5에서 명시적 새로고침·truncation·stale-result guard를 검증했지만 깊이/파일 수 제한과 진행률·취소는 남아 있다. |
| F-08 실제 macOS 키보드 검증 | 부분 | [task 7](.omo/evidence/code-review-remediation/task-7.md) 및 task 8의 브라우저 native-contract equivalent가 키보드/IME/탭 흐름을 검증한다. 산출물은 실제 WKWebView 네이티브 캡처라고 과장하지 않으며, 독립된 네이티브 GUI 증거는 남은 경계다. |
| F-09 빈 상태·첫 작업 안내 | 해결 | [task 6](.omo/evidence/code-review-remediation/task-6.md)의 clean final-tab close가 `tabCount:0`, `welcome:true`를 직접 기록해 빈 상태 표면을 검증한다. [task 8](.omo/evidence/code-review-remediation/task-8.md)는 실제로 `normal.editor-viewer-workspace-editor` 전환과 `list-reject.*` 탐색기 오류·재시도를 검증하지만, 빈 workspace나 탐색기 진입 CTA의 증거로는 사용하지 않는다. |
| F-10 오류 후 복구 경로 | 해결 | task 4의 삭제/읽기 불가·저장 보존 계약과 task 8의 삭제 recovery 캡처가 취소·재시도·명시적 close 경계를 검증했다. |
| Q-01 CSS minifier 경고 | 해결 | [task 1](.omo/evidence/code-review-remediation/task-1.md)의 old-marker red probe와 fresh build가 경고 0건을 기록했다. |
| Q-02 초기 번들 크기 | 보류 | [task 10](.omo/evidence/code-review-remediation/task-10.md)이 초기/뷰어 비용을 측정했고 기존 dynamic import를 확인했다. 최적화·threshold 변경은 별도 성능 결정 전까지 하지 않는다. |
| Q-03 Rust 가시성·의존성 | 부분 | [task 11](.omo/evidence/code-review-remediation/task-11.md)의 fresh audit는 production advisory 3개 노드와 `cargo check`의 기존 visibility warning을 기록했다. 업그레이드/lockfile 변경은 명시적 승인 뒤다. |
| Q-04 mtime 외부 변경 감지 | 부분 | [task 12](.omo/evidence/code-review-remediation/task-12.md)의 bounded `(path, mtime, size)` 신호가 입증된 same-mtime changed-size 및 cross-tab miss를 고쳤다. same-path·same-mtime·same-size rewrite는 입증되지 않아 구현했다고 주장하지 않는다. |

**현재 집계:** 해결 7건(F-02, F-03, F-04, F-06, F-09, F-10, Q-01), 부분 5건(F-01, F-07, F-08, Q-03, Q-04), 보류 2건(F-05, Q-02). 보류/부분 항목은 미해결 작업이며, 이 문서는 전체 해결을 선언하지 않는다.

**후속 결정 링크:** 본문 검색은 [2026-08-11 검색 결정](docs/reviews/full-text-search-decision-2026-08-11.md), 실측 성능 기준선은 [task 10](.omo/evidence/code-review-remediation/task-10.md), 의존성 advisory는 [task 11](.omo/evidence/code-review-remediation/task-11.md), watcher characterization 및 입증된 bounded fix는 [task 12](.omo/evidence/code-review-remediation/task-12.md)에 기록되어 있다. 각 문서는 승인·측정·잔여 위험의 범위를 독립적으로 보존한다.

### 2026-08-11 F2 후속 재검증 범위

Recent Documents와 File Finder의 현재 문서 전환은 `openDocumentSafely`의 read → 저장 준비 → watcher detach/attach → mount 트랜잭션을 사용한다. 두 경로 각각의 detach 거부 시나리오는 A 편집기·A 탭·A watcher를 유지하고, 늦은 pathless A 이벤트가 B를 오염시키지 않는지 `tests/main-wiring.test.ts`에서 검증한다. 문서 없는 Explorer/Recent/File Finder 진입은 source-occurrence 수 대신 실제 reload URL의 file·global vault·root 파라미터를 검증한다.

최근 문서에서 읽을 수 없는 경로는 자동 제거하지 않는다. 복구 모달의 재시도를 위해 목록에 남기는 현재 동작을 통합 테스트로 고정했고, 사용되지 않던 `pruneMissing` 순수 helper와 그 구현 전용 단위 테스트는 제거했다.

이 후속 범위는 TypeScript/브라우저 wiring뿐이며 Rust FFI는 수정하거나 재검증하지 않았다. 검증 호스트는 Darwin arm64이고 installed nightly components에 `cargo-miri`가 없으므로 Miri를 실행하지 않았다. Windows `ReplaceFileW` FFI도 Windows runner·linker·runtime이 없는 macOS 호스트에서는 실행하지 않았다. 이는 FFI 안전성의 통과 증거가 아니며, 해당 경계는 Windows 환경과 Miri가 설치된 적절한 Rust 도구체인에서 별도로 검증해야 한다.

### 2026-08-09 기능별 구현 우선순위(P0~P2)와 예상 작업 규모 (역사적 스냅샷)

아래 표는 기능별로 `구현 우선순위`와 `예상 작업 규모`를 별도 판단한 결과다. 우선순위는 사용자 흐름을 막는 정도와 선행 의존성을 기준으로 P0(즉시 결정·착수), P1(핵심 개선), P2(후속 품질 보강)으로 구분했다. 규모는 현재 단일 문서 모델을 볼트 워크스페이스로 확장할 때 필요한 프론트엔드·Rust 경계·수동 검증을 함께 포함한 상대적 추정이다.

| 기능 | 구현 우선순위 | 예상 작업 규모 | 우선 구현 이유 | 관련 발견 |
| --- | --- | --- | --- | --- |
| 볼트 선택·workspace root 영속화 | P0 | 중 | 모든 파일 열기·탐색기·검색의 기준 경로를 먼저 고정해야 함 | F-02, F-09 |
| 탭·탭별 편집 상태 | P0 | 대 | 여러 문서 비교와 전환이라는 핵심 작업 흐름을 직접 차단함 | F-01 |
| 탐색기 권한 오류·재시도 | P1 | 소~중 | 접근 불가 폴더를 빈 폴더로 오인하는 즉시성 높은 오류를 해소함 | F-06 |
| 파일 열기 오류 안내·복구 | P1 | 소 | 실패 원인과 재시도/다른 파일 열기 경로를 제공함 | F-03, F-10 |
| 자동저장 통합 신뢰성 | P1 | 중 | 정상 저장 주기와 파일 전환·종료 경계의 디스크 반영을 보장함 | F-04 |
| 파일 찾기·본문 검색 범위 | P1 | 중~대 | 현재 파일명 검색과 사용자가 기대하는 내용 검색의 간극을 해소함 | F-05 |
| 대용량 폴더 스캔·캐시 최신성 | P1 | 중~대 | 큰 볼트에서 결과 누락과 초기 반응성 저하를 줄임 | F-07 |
| 외부 변경·충돌 복구 | P1 | 중 | 파일 삭제·권한 변경·외부 수정 뒤의 다음 행동을 표준화함 | F-10, Q-04 |
| 빈 상태·첫 작업 안내 | P1 | 중 | 최근 문서가 없는 첫 실행에서도 볼트를 선택하고 작업을 시작하게 함 | F-09 |
| 탐색기·단축키 실제 macOS 동작 | P2 | 소 | DOM 계약 이후 WKWebView 포커스와 Cmd 단축키 회귀를 고정함 | F-08 |

## 2. 역사적 기능 완전성 검토 (2026-08-09 기준)

> 아래 F-01~F-10 원문은 당시의 발견과 근거를 보존한다. 현재 판정은 위의 2026-08-11 표를 참조한다.

각 항목은 `분류 / 심각도 / 제목 / 근거 / 사용자 영향 / 개선 방향 / 우선순위 / 규모 / 검증 여부` 순서로 기록했으며, 모든 발견사항은 실제 코드 위치 또는 실행 결과를 근거로 삼고 바로 적용 가능한 개선 방향을 함께 제시했다.

### 핵심 흐름별 판정 요약

| 흐름 | 현재 부족한 점 | 심각도 | 사용자 영향 | 관련 발견 |
| --- | --- | --- | --- | --- |
| 파일 열기 | 열기 실패가 원인별 행동 안내 없이 한 줄 오류로 끝남 | 중간 | 삭제·권한·인코딩 문제에서 다음 행동을 결정하기 어려움 | F-03 |
| 저장·자동저장 | debounce 저장은 있으나 전환·종료 직전의 실제 디스크 반영을 macOS 통합 경로에서 보장하지 않음 | 중간 | 저장됐다고 믿은 내용이 파일에 반영됐는지 확인하기 어려움 | F-04 |
| 탭 | 탭과 탭별 편집 상태가 없음 | 치명적 | 여러 노트를 비교하거나 오갈 때 창을 여러 개 써야 하고 현재 문서가 교체됨 | F-01 |
| 검색 | 파일명·상대 경로만 찾고 본문·태그 검색은 없음 | 중간 | 볼트 내용으로 원하는 노트를 찾지 못함 | F-05 |
| 탐색기 | 권한 오류가 빈 폴더처럼 보이고 목록 캐시가 최신 상태를 보장하지 않음 | 높음 | 접근 불가·삭제된 폴더를 빈 폴더로 오인하거나 새 파일을 놓침 | F-06, F-07 |
| 볼트 전환 | 명시적인 볼트 폴더 선택·영속 workspace root가 없고 문서 폴더에 종속됨 | 치명적 | 작업 공간을 선택·유지할 수 없어 문서가 바뀔 때 탐색기 맥락도 바뀜 | F-02, F-09 |
| 외부 변경 | 단일 파일 watcher와 mtime 기준에 의존하며 탭·볼트 단위 충돌 모델이 없음 | 높음 | 외부 수정 감지·충돌 대응이 현재 문서 하나에 한정되고 일부 파일시스템에서는 변경을 놓칠 수 있음 | Q-04, F-01 |

### 자연스럽게 기대되는 동작의 평가 범위

| 기대 동작 | 확인한 구현/실행 근거 | 판정 및 우선순위 |
| --- | --- | --- |
| 오류 표시와 복구 | 파일 열기 실패(`src/main.ts:1133-1140`), 저장 충돌(`src/editor.ts:228-286`), watcher의 삭제·읽기 실패(`src-tauri/src/watcher.rs:105-122`) | 오류 유형별 재시도·다시 읽기·다른 경로 열기가 일관되지 않음. F-03/F-10, P1 |
| 빈 상태 | 시작 화면(`src/main.ts:1111-1129`), 검색 결과의 빈 목록(`src/sidebar/search/search-panel.ts:224-245`) | 빈 폴더와 권한 오류를 구분하고 볼트 선택으로 이어지는 안내가 부족함. F-06/F-09, P1 |
| 키보드 조작 | 탐색기 roving tabindex 및 방향키/Home/End/Enter(`src/sidebar/explorer/explorer-panel.ts:275-326,714-760`), 관련 단위 테스트 | DOM 계약은 있으나 실제 WKWebView의 포커스·Cmd 단축키 경로는 미검증. F-08, P2 |
| 대용량 폴더 | recursive scan의 깊이 12·파일 10,000개 제한(`src-tauri/src/commands.rs:726-744`), 탐색기/검색 캐시(`src/sidebar/explorer/explorer-panel.ts:335-349`, `src/sidebar/search/search-panel.ts:261-280`) | 부분 결과·초기 스캔 비용·변경 후 최신성 문제가 있음. F-07, P1 |

위 네 가지는 단순한 구현 세부가 아니라 실패 시 사용자가 다음 행동을 결정할 수 있는지, 비어 있는 상태를 오류로 오인하지 않는지, 마우스 없이 핵심 흐름을 완료할 수 있는지, 큰 볼트에서도 결과를 신뢰할 수 있는지를 기준으로 평가했다. 저장되지 않은 편집 내용의 복구는 범위에서 제외하고, 자동저장은 주기와 정상 저장 동작만 F-04에서 평가했다.

### F-01. 볼트 워크스페이스와 탭이 없음

- 분류: 기능
- 심각도: 치명적
- 제목: 문서 1개-창 1개 모델이 볼트 작업 흐름을 막음
- 근거: `README.md:72-77`은 폴더/볼트 사이드바와 탭을 의도적으로 제외하고, `docs/superpowers/specs/2026-06-10-mermark-design.md:22-26`도 같은 항목을 non-goal로 둔다. 실제 현재 문서는 `src/main.ts:740-823`의 단일 `current` 에디터를 새 문서로 교체하는 구조다.
- 사용자 영향: 여러 노트를 비교하거나 오가려면 창을 여러 개 띄워야 한다. 현재 탐색기에서 파일을 열어도 탭이 추가되는 것이 아니라 기존 문서가 교체되므로, 볼트의 맥락과 작업 순서를 유지하기 어렵다.
- 개선 방향: `VaultWorkspace`, `Tab`, `activeTabId`를 명시적으로 도입한다. 탭마다 파일 경로, 에디터 상태, 스크롤/커서, 뷰어 상태를 보유하고, 파일 전환 전에 현재 탭의 자동저장만 await한다. 닫기 시 dirty 상태와 중복 경로 정책도 먼저 정의한다.
- 우선순위: P0
- 예상 규모: 대
- 검증 여부: true (정적 분석; README/설계/실제 단일 `current` 구조로 확인)

### F-02. 볼트 선택과 루트 영속성이 없음

- 분류: 기능
- 심각도: 높음
- 제목: 탐색기 루트가 현재 문서 폴더에 종속됨
- 근거: `src/main.ts:815-823`에서 문서를 열 때 탐색기 루트를 `currentBaseDir`로 재설정하고, `src/sidebar/explorer/explorer-panel.ts:453-468`에서 탐색기 루트를 그 시점의 폴더로 정규화한다. 시작 화면의 폴더 CTA도 `src/main.ts:1111-1127`에서 native folder picker가 아니라 탐색기 토글을 호출한다.
- 사용자 영향: 사용자가 작업 중인 볼트를 한 번 선택해 유지하거나, 문서가 없는 새 시작 상태에서 볼트를 지정하는 흐름이 없다. 문서가 다른 폴더로 바뀌면 탐색기 컨텍스트도 바뀐다.
- 개선 방향: native 폴더 선택으로 workspace root를 정하고, 사용자 설정에 경로를 저장한다. 문서 폴더와 workspace root를 별도 개념으로 유지하고, 경로가 없어졌거나 권한이 없을 때 재선택 CTA를 제공한다.
- 우선순위: P0
- 예상 규모: 중
- 검증 여부: true (정적 분석 및 실제 실행의 시작 화면/문서 열기 경로 확인)

### F-03. 파일 열기·저장 경로는 있으나 명시적 사용자 피드백이 약함

- 분류: 기능
- 심각도: 중간
- 제목: 초기 열기 오류가 영어 단문에 머무름
- 근거: `src/main.ts:1133-1140`은 `read_file` 실패를 `Failed to open: ...` 한 줄로 표시한다. 반면 저장 상태는 `src/main.ts:118-151`에서 저장 중/저장됨/충돌/실패를 별도로 표시한다.
- 사용자 영향: 파일이 삭제되었거나 권한이 없거나 인코딩이 맞지 않는 경우 원인과 다음 행동이 분명하지 않다. 한국어 UI 흐름과도 일관되지 않는다.
- 개선 방향: 오류 종류를 읽기 실패, 권한, 존재하지 않는 경로로 분류하고, 다시 시도·탐색기 열기·다른 파일 열기 행동을 제공한다. Rust 오류는 사용자 메시지와 진단용 상세를 분리한다.
- 우선순위: P1
- 예상 규모: 소
- 검증 여부: true (정적 분석; 존재하지 않는 경로로 Tauri 실행해 오류 화면 경로 기동 확인)

### F-04. 자동저장 주기는 구현되어 있으나 정상 동작 신뢰 신호를 더 검증해야 함

- 분류: 기능
- 심각도: 중간
- 제목: debounce 저장과 실제 macOS 파일 반영의 통합 검증 공백
- 근거: `src/editor.ts:157-168`은 편집 후 debounce로 저장하고, `src-tauri/src/commands.rs`의 `write_file`은 임시 파일 후 rename 방식이다. 기본 설정은 `src/settings/app.ts:325`의 `autosaveDelaySetting`에서 관리되며, 테스트는 `tests/editor-autosave.test.ts`와 `tests/settings-app.test.ts`에 있다.
- 사용자 영향: 저장 표시가 보여도 앱 종료 직전, 빠른 연속 입력, 다른 파일로 즉시 전환할 때 실제 파일과 UI 상태가 일치하는지 사용자가 알기 어렵다.
- 개선 방향: macOS 통합 시나리오를 추가한다: 편집 → 기본 주기 대기 → 파일 바이트 확인, 저장 중 파일 전환, 읽기 전용/권한 오류, 강제 저장 정책. 상태 표시에는 마지막 성공 시각 또는 대상 파일명을 노출한다.
- 우선순위: P1
- 예상 규모: 중
- 검증 여부: true (정적 분석 및 기존 자동저장 단위 테스트 통과; 이번 실행에서는 UI 입력/파일 바이트까지 수동 확인하지 않음)

### F-05. 파일 검색은 이름 검색이며 내용 검색이 아님

- 분류: 기능
- 심각도: 중간
- 제목: 검색이라는 기대와 파일 찾기의 범위가 다름
- 근거: `src/sidebar/search/search-panel.ts:7-20`은 파일 찾기를 현재 탐색기 루트의 recursive scan으로 정의하고, `src/sidebar/search/search-panel.ts:224-245`는 `rel_path`를 fuzzy rank한다. Rust 경로 `src-tauri/src/commands.rs:726-900`도 파일 목록만 반환하며 본문을 읽지 않는다.
- 사용자 영향: 사용자는 볼트 안의 본문, 제목, 태그를 검색한다고 기대할 수 있지만 현재는 파일명/상대 경로만 찾는다. 원하는 노트를 찾지 못해 탐색기와 파일 찾기를 반복하게 된다.
- 개선 방향: 기능 명칭을 “파일 찾기”로 명확히 하거나, 별도 본문 검색을 추가한다. 본문 검색은 debounce, 결과 수 제한, UTF-8 오류 처리, 대용량 파일 제외 정책, 결과에서 해당 줄로 이동하는 계약을 먼저 정한다.
- 우선순위: P1
- 예상 규모: 중~대
- 검증 여부: true (정적 분석 및 관련 테스트 통과)

### F-06. 탐색기·파일 찾기의 권한 오류와 빈 상태가 구분되지 않음

- 분류: 기능
- 심각도: 높음
- 제목: 탐색기에서 실패한 폴더가 빈 폴더처럼 보임
- 근거: `src/sidebar/explorer/explorer-panel.ts:335-349`는 `listDir` rejection을 빈 배열로 바꾸고 그 빈 결과를 캐시한다. 파일 찾기는 `src/sidebar/search/search-panel.ts:207-214`에서 오류를 별도 표시하지만, 탐색기는 같은 수준의 오류 행이 없다.
- 사용자 영향: 권한 거부, 네트워크 볼륨 단절, 삭제된 폴더를 “파일이 없는 폴더”로 오인한다. 한 번 실패하면 캐시 때문에 다시 열어도 즉시 회복되지 않는다.
- 개선 방향: 오류 상태를 폴더 노드에 저장하고 “읽을 수 없음 / 다시 시도”를 표시한다. 재시도 시 해당 경로 캐시를 무효화하고, root 자체 오류는 workspace 재선택 CTA로 연결한다.
- 우선순위: P1
- 예상 규모: 소~중
- 검증 여부: true (정적 분석; `tests/explorer-panel` 계열 테스트는 성공적인 목록 중심)

### F-07. 큰 폴더를 방어하지만 결과의 완전성과 최신성이 제한됨

- 분류: 기능 / 성능
- 심각도: 높음
- 제목: 10,000개·깊이 12 제한과 탐색기 캐시가 큰 볼트 경험을 제한함
- 근거: `src-tauri/src/commands.rs:726-744`는 recursive 파일 찾기를 깊이 12, 파일 10,000개로 제한하고, `src/sidebar/search/search-panel.ts:261-280`은 패널을 열 때 한 번 스캔한 결과를 캐시한다. 탐색기도 `src/sidebar/explorer/explorer-panel.ts:335-349`에서 폴더 목록을 캐시한다.
- 사용자 영향: 제한을 넘은 파일은 경고가 있더라도 찾을 수 없고, 열린 뒤 생성·이동된 파일은 다시 열기 전까지 보이지 않는다. 큰 볼트에서 초기 스캔과 렌더링이 UI 반응성을 떨어뜨릴 수 있다.
- 개선 방향: 스캔을 worker/백엔드 작업으로 분리하고 진행률과 취소를 제공한다. 파일 시스템 이벤트 또는 명시적 새로고침으로 캐시를 갱신하고, 부분 결과에는 기준(깊이/개수)과 재스캔 행동을 함께 보여준다. 제한값은 측정 후 조정한다.
- 우선순위: P1
- 예상 규모: 중~대
- 검증 여부: true (정적 분석; truncation 경고 렌더링 로직과 관련 테스트 통과)

### F-08. 키보드 접근성 구조는 좋지만 실제 앱 경로 검증이 부족함

- 분류: 기능 / 품질
- 심각도: 중간
- 제목: 탐색기 키보드 계약은 구현됐지만 Tauri 수동 확인이 없음
- 근거: `src/sidebar/explorer/explorer-panel.ts:275-326,714-760`은 roving tabindex와 방향키/Home/End/Enter를 구현한다. 테스트에는 `tests/sidebar-panels.test.ts`, `tests/sidebar-sash.test.ts`, `tests/explorer-panel.test.ts`가 있다.
- 사용자 영향: 단위 테스트가 보장하는 DOM 계약과 실제 WKWebView의 포커스 이동, Cmd/Enter, IME, 시스템 단축키 충돌이 다를 수 있다.
- 개선 방향: macOS 앱에서 탐색기 열기 → Tab → 방향키로 폴더 확장 → Enter로 파일 열기 → Cmd+Enter 새 창을 확인하는 smoke 시나리오를 추가하고, 포커스가 문서 편집기와 사이드바 사이에서 잃어버려지지 않는지 기록한다.
- 우선순위: P2
- 예상 규모: 소
- 검증 여부: 부분적 (정적 분석/단위 테스트 true, 실제 UI 상호작용은 미검증)

### F-09. 빈 상태는 시작 화면이 있으나 볼트 중심 안내가 아님

- 분류: 기능
- 심각도: 중간
- 제목: 문서가 없는 시작 상태에서 작업공간을 만들 수 없음
- 근거: `src/main.ts:1111-1129`의 welcome pane은 최근 문서/즐겨찾기와 탐색기 토글을 제공한다. 폴더 선택을 위한 별도 native picker나 workspace root 설정 명령은 없다.
- 사용자 영향: 첫 실행 사용자가 “어떤 폴더를 작업공간으로 열어야 하는가”를 결정하기 어렵고, 즐겨찾기/최근 목록이 비어 있으면 다음 행동이 탐색기 토글에 숨겨진다.
- 개선 방향: “볼트 폴더 열기”를 첫 CTA로 두고, 선택 후 탭/탐색기/최근 문서를 workspace 상태로 초기화한다. 접근 불가·빈 폴더·숨김 파일 정책도 안내한다.
- 우선순위: P1
- 예상 규모: 중
- 검증 여부: true (정적 분석; 실제 `README.md` 문서로 앱 기동 성공 확인)

### F-10. 오류 후 복구 경로가 상태별로 일관되지 않음

- 분류: 기능 / 품질
- 심각도: 높음
- 제목: 저장 실패·외부 변경·탐색기 읽기 실패의 복구 행동이 서로 다르고 일부는 재시도 지점이 없음
- 근거: 저장 충돌 시 `src/editor.ts:228-286`은 `pause` 정책에서 자동저장을 멈추고 상태만 `conflict`로 남기며, `reloadFromFile` 또는 overwrite 선택은 호출자가 연결해야 한다. 외부 watcher는 `src-tauri/src/watcher.rs:105-122`에서 파일이 사라졌거나 읽히지 않으면 이벤트 없이 반환한다. 초기 파일 열기 실패는 `src/main.ts:1133-1140`에서 문자열만 교체하고, 탐색기 폴더 읽기 실패는 F-06처럼 빈 목록으로 캐시된다.
- 사용자 영향: 사용자는 “저장 실패”, “파일이 외부에서 바뀜”, “파일이 없어짐”, “폴더 접근 불가”를 구분해도 복구할 수 있는 버튼이나 다음 행동을 같은 위치에서 찾기 어렵다. 특히 watcher가 파일 삭제/권한 변경을 조용히 삼키면 편집 화면이 마지막 상태에 머물러 파일 상태와 어긋날 수 있다.
- 개선 방향: 오류 상태를 `open/read`, `save`, `external-change`, `workspace-list`로 표준화하고 각 상태에 다시 시도, 디스크에서 다시 읽기, 다른 경로 열기, 폴더 재선택을 연결한다. 파일이 사라진 경우에도 현재 문서를 즉시 폐기하지 말고 “파일 찾기/다른 이름으로 저장” 같은 운영 복구를 제공한다. 단, 이 리뷰 범위에서는 저장되지 않은 편집 내용의 복구 설계는 평가하지 않는다.
- 우선순위: P1
- 예상 규모: 중
- 검증 여부: true (정적 분석 및 `tests/editor-autosave.test.ts`의 pause/reload/forceSave 경로 확인; 실제 오류 버튼 클릭은 미검증)

## 3. 역사적 품질 및 성능 검토 (2026-08-09 기준)

> 아래 Q-01~Q-04 원문은 당시의 발견과 근거를 보존한다. 현재 판정과 후속 결정은 위의 2026-08-11 표 및 링크를 참조한다.

이 절은 앞 절의 기능 누락(F-01~F-10)과 섞이지 않도록, 현재 기능이 동작한다는 전제에서 유지보수성·빌드 신뢰성·초기 로드 비용·변경 감지 신뢰성만 별도로 평가한다. 각 항목은 기능 추가 자체가 아니라 코드와 실행 품질에 미치는 위험을 기준으로 분류했다.

### Q-01. CSS 주석이 minifier 경고를 발생시킴

- 분류: 품질
- 심각도: 중간
- 제목: CSS 주석 안의 `*/` 패턴이 빌드 경고를 유발
- 근거: `src/styles.css:429-433`의 주석 예시에는 `***x***/`와 `/*__x__*` 같은 토큰이 포함되어 있다. `npm run build`에서 CSS minify 단계에 `Unexpected "*"`, `Unterminated string token` 경고가 발생했다.
- 사용자 영향: 현재 빌드는 성공하지만 minifier가 주석을 CSS 문법으로 오해한다. 도구 버전 변경 시 빌드 실패나 주석 이후 스타일 손상이 될 수 있다.
- 개선 방향: 주석 예시에서 `*/`가 만들어지는 문자열을 제거하거나 코드 표기로 바꾸고, 경고 0건을 CI 기준으로 둔다.
- 우선순위: P1
- 예상 규모: 소
- 검증 여부: true (빌드 로그로 재현)

### Q-02. 초기 번들이 무겁고 Mermaid/뷰어 코드가 대형 청크로 남음

- 분류: 성능
- 심각도: 높음
- 제목: 첫 로드 비용과 메모리 비용이 기능 범위에 비해 큼
- 근거: `npm run build` 결과 `dist/assets/index-BgbX6OxP.js` 약 994 kB, Mermaid 약 622 kB, Wardley 약 615 kB, xlsx 약 500 kB, PDF 약 477 kB이며 500 kB 초과 청크 경고가 발생했다. `src/main.ts:398` 이후 여러 viewer를 초기 부트스트랩에서 등록한다.
- 사용자 영향: 문서 하나를 여는 사용자도 사용하지 않는 PDF/XLSX/EPUB/HWP/SQLite/대형 Mermaid 경로의 파싱·캐시 비용을 부담할 수 있다. 저사양 Mac에서 첫 화면과 메모리 압박이 커진다.
- 개선 방향: viewer와 다이어그램 문법을 실제 확장자/언어 사용 시 dynamic import하고, 초기 셸·Markdown·기본 테마만 eager load한다. 성능 예산을 initial JS gzip, first paint, 문서 오픈 시간으로 수치화한다.
- 우선순위: P1
- 예상 규모: 중
- 검증 여부: true (production build 산출물 크기로 확인)

### Q-03. Rust 공개 가시성과 의존성 상태에 경고가 남음

- 분류: 품질
- 심각도: 낮음
- 제목: Rust private interface 경고와 npm 취약점 보고가 남음
- 근거: Tauri 실행 시 `src-tauri/src/epubview.rs:312,364`에 `EpubOriginBase`의 가시성보다 `epub_frame_csp`가 넓다는 `private_interfaces` 경고가 발생했다. `npm ci` 결과 6 vulnerabilities(중간 2, 높음 4)가 보고됐다.
- 사용자 영향: 즉시 기능을 막지는 않지만 공개 경계가 흐리고, 의존성 취약점이 계속 누적될 수 있다.
- 개선 방향: 함수/타입의 가시성을 일치시키고 `npm audit` 결과를 패키지별로 triage한다. 자동 수정은 lockfile 영향과 런타임 회귀를 검토한 뒤 적용한다.
- 우선순위: P2
- 예상 규모: 소~중
- 검증 여부: true (Tauri/npm 설치 출력으로 확인)

### Q-04. 외부 변경 감지는 설계되어 있으나 mtime 의존성이 있음

- 분류: 품질
- 심각도: 중간
- 제목: 파일 변경 판단이 수정 시각 해상도에 의존
- 근거: `src/document/file-watch.ts:14-22`는 외부 변경 시 unsaved 여부로 reload/conflict를 결정하고, Rust `write_file` 구현은 mtime baseline을 비교한다. 구현 주석 자체도 coarse-resolution filesystem에서 같은 시간 구간의 외부 수정이 누락될 수 있음을 설명한다.
- 사용자 영향: 최신 macOS APFS에서는 가능성이 낮지만, 파일 시스템/도구 조합에 따라 외부 변경이 감지되지 않을 수 있다.
- 개선 방향: mtime만이 아니라 파일 크기와 빠른 해시 또는 watcher 이벤트 후 실제 내용 비교를 보조 신호로 사용한다. 비교 비용과 대용량 문서 정책을 측정해 선택한다.
- 우선순위: P2
- 예상 규모: 중
- 검증 여부: true (정적 분석; 외부 변경 실제 충돌 UI는 이번 실행에서 미검증)

## 4. 역사적 실행 및 정적 검증 기록 (2026-08-09 기준)

> 이 명령 결과와 한계는 당시 실행의 기록이다. task 1 및 task 8–12의 최신 산출물은 위의 현재 상태 표와 각 링크를 기준으로 한다.

### 명령 결과

- Bun 우선 확인: `bun --version`은 `1.3.6`을 반환했다. 그러나 `bun test`는 Bun 기본 런타임에서 `window`/`document`가 제공되지 않아 1,957개 중 996개 통과, 961개 실패, 6개 오류로 종료됐다. 따라서 브라우저 DOM 테스트를 지원하는 npm/Vitest를 검증 명령으로 사용했다.
- `npm test` 성공: Vitest에서 120개 테스트 파일과 2,035개 테스트가 모두 통과했다. 자동저장 관련 테스트도 이 전체 실행에 포함된다.
- `npm run build` 성공: TypeScript 검사와 Vite production build가 성공했다. 단, CSS minify 단계에서 `Unexpected "*"`/`Unterminated string token` 경고가 재현됐고, `index-BgbX6OxP.js` 993.53 kB 및 Mermaid/Wardley/XLSX/PDF 등 500 kB 초과 청크 경고가 있었다.
- `npm run tauri dev -- README.md` 실제 macOS 실행: Vite dev server가 `http://localhost:1420/`에 기동되고 Rust Tauri 앱이 컴파일된 뒤 `target/debug/mermark README.md` 프로세스가 실행됐다. 실행 로그에서 `src-tauri/src/epubview.rs`의 `private_interfaces` 경고도 확인했으며, 기동 상태를 확인한 뒤 Ctrl-C로 종료했다.
- 존재하지 않는 파일 오류 경로는 `src/main.ts:1133-1140`의 초기 열기 실패 분기와 관련 단위 테스트로 정적 검증했다. 이번 실제 GUI 실행에서는 정상 `README.md` 기동까지만 확인했다.

### 실행 검증의 한계

이번 실행은 macOS Tauri 프로세스 기동과 정상 `README.md` 인자 전달까지 확인했다. `bun test`는 DOM 환경 부재로 전체 검증에 사용할 수 없어 `npm test`로 대체했고, 전체 Vitest 테스트는 통과했다. GUI 자동화 도구를 붙인 상호작용 테스트는 수행하지 않았으므로, 실제 WKWebView에서의 탭 이동, 탐색기 키보드, Cmd 단축키, 자동저장 후 디스크 바이트, 외부 변경 모달은 정적 분석과 단위 테스트 결과로만 판단했다. 다음 단계에서 위 시나리오를 macOS smoke/E2E로 고정해야 한다.

## 5. 역사적 범위 확인 (2026-08-09 기준)

이번 리뷰에서 소스 코드, 테스트 코드, 설정 파일은 수정하지 않았다. 산출물은 저장소 루트의 이 문서 하나다. `node_modules`, `dist`, Rust target은 검증 명령이 생성한 로컬 산출물이며 리뷰 내용의 소스 변경으로 취급하지 않는다.
